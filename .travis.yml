language: python
python:
  - "3.6"
  - "3.7"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - py.test  --cov-report term --cov=app/test/

env:
  - CODECOV_TOKEN=2edb5931-be93-4532-ba09-2beacb3d7817 #IF ONLY YOU HAVE A PRIVATE REPOSITORY

after_success:
  - codecov
